{
  "name": "senv-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm --filter senv run test",
    "publish:npm": "node scripts/publish-npm.mjs",
    "publish:npm:dry-run": "node scripts/publish-npm.mjs --dry-run"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}

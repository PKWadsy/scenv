{
  "name": "senv-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm --filter senv run test"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
